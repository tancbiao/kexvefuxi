# 白龙：Q版角色换装系统改进需求

## 背景

首页 `index.html` 右下角已有一套 Q 版可拖动人物 + 装备换装系统。
但目前角色是**纯CSS绘制的火柴人**（一个橙黄色方块+圆脑袋），视觉上非常简陋。
我们刚刚用 AI 生成了两张高颜值的 Q 版角色底稿，目标是替换掉现有角色。

---

## 一、现状分析

### 当前系统（index.html 第 689-862 行）

**角色本体**：`char-base` div，纯 CSS 绘制，黄色方块+圆形脑袋
**装备**：6个装备槽位（hat/top/bottom/cape/face/eyes），用 Maplestory sprite 图标
**交互**：可拖动位置 + 点击"换装"按钮打开面板选择装备
**装备来源**：GitHub CDN 上的冒险岛 sprite 图标

### 资源文件
```
_kexvefuxi/maplestory_sprites/
├── 帽子/    (18个图标)
├── 上衣/    (14个图标)
├── 裤裙/    (12个图标)
├── 披风/    (5个图标)
├── 脸饰/    (9个图标)
└── 眼饰/    (4个图标)
```

### AI新生成的底稿（已保存在 ComfyUI output）
```
D:\ComfyUI\output\
├── dresscode_girl_base_00001_.png   # Q版女孩，白色基础装，白背景
└── dresscode_boy_base_00001_.png     # Q版男孩，白色基础装，白背景
```

---

## 二、需要白龙做的事

### 【任务1】替换角色底图为AI绘制的高颜值角色

**目标**：把 `char-base` div 换成真正的 AI 绘制角色图

**方案A（推荐）**：用 AI 角色作为主体
1. 将 `dresscode_girl_base_00001_.png` 和 `dresscode_boy_base_00001_.png` 上传到 GitHub
2. 让用户选择角色性别（界面加两个按钮：男/女）
3. 选中后加载对应的角色底图作为 `.char-base`
4. 装备仍然叠加在角色上方

**方案B**：用 ComfyUI 提取角色轮廓做遮罩
- 用 rembg 或 ComfyUI 的 PersonMask 提取角色透明背景
- 在角色上方叠加装备层

**修改文件**：`index.html`

**参考代码结构**（现有换装逻辑不变，只需替换 char-base）：
```javascript
// 新增：角色选择
const CHAR_BASE_URL = {
  girl: 'https://raw.githubusercontent.com/tancbiao/kexvefuxi/main/char_imgs/girl_base.png',
  boy: 'https://raw.githubusercontent.com/tancbiao/kexvefuxi/main/char_imgs/boy_base.png'
};
let currentChar = 'girl'; // 默认女孩

// 替换 char-base div 中的 CSS 绘制，改为 <img>
function setCharBase(gender) {
  currentChar = gender;
  const base = document.getElementById('charBase');
  base.innerHTML = `<img src="${CHAR_BASE_URL[gender]}" style="width:100%;height:100%;object-fit:contain;">`;
}
```

---

### 【任务2】为角色添加动画

**目标**：让角色"活"起来，增加游戏感

**建议的动画效果**：

1. **站立待机动画**（最基础）
   - 轻微上下浮动（translateY 0~4px）
   - 周期 2s，ease-in-out 循环

2. **答题正确后**
   - 短暂放大弹跳（scale 1 → 1.2 → 1）
   - 配合星星✨粒子效果

3. **答题错误后**
   - 左右摇晃（rotate -5deg → 5deg）
   - 颜色变红一下

4. **装备穿戴动画**
   - 新装备出现时从大变小（scale 1.5 → 1）配合淡入

**参考 CSS**：
```css
/* 待机呼吸动画 */
@keyframes float {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-4px); }
}
.char-base {
  animation: float 2s ease-in-out infinite;
}

/* 答对弹跳 */
@keyframes bounce {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.2); }
}
.char-bounce { animation: bounce 0.4s ease; }

/* 答错摇晃 */
@keyframes shake {
  0%, 100% { transform: rotate(0deg); }
  25% { transform: rotate(-5deg); }
  75% { transform: rotate(5deg); }
}
.char-shake { animation: shake 0.3s ease; }
```

**答题动画触发**：在 `checkAnswer` 函数中，根据答对/答错调用对应动画：
```javascript
function checkAnswer(selected) {
  const correct = currentAnswer === selected;
  const charBase = document.getElementById('charBase');
  
  if (correct) {
    charBase.classList.remove('char-bounce');
    void charBase.offsetWidth; // 触发重排，重启动画
    charBase.classList.add('char-bounce');
    spawnParticles(charBase, '✨', 5);
  } else {
    charBase.classList.remove('char-shake');
    void charBase.offsetWidth;
    charBase.classList.add('char-shake');
  }
}
```

---

### 【任务3】让装备和角色更协调

**问题**：冒险岛 sprite 图标风格是像素化的，和 AI 绘制的细腻角色不在同一画风

**可选方案**：

方案A：**继续用冒险岛图标**（省事，保持现有风格一致）
- 冒险岛本身就是卡通像素风，和 Q 版角色比较搭
- 可以适当缩小装备图标尺寸，避免遮挡角色主体

方案B：**用 ComfyUI 生成配套装备**
- 生成与角色同画风的透明背景装备图
- 上传到 GitHub 替换现有 sprite

**建议**：先采用方案A（保持现有代码简单），后期迭代时再考虑方案B

---

### 【任务4】（可选）让换装面板更美观

**当前问题**：装备缩略图太小（36x36px），难以看清

**改进方向**：
- 放大缩略图到 48x48px
- 添加 hover 放大预览
- 按稀有度（颜色）分组显示装备

---

## 三、技术要点

### 文件路径
- `index.html` — 首页，包含完整换装系统（第689-862行）
- `maplestory_sprites/` — 装备图标目录
- GitHub CDN：`https://raw.githubusercontent.com/tancbiao/kexvefuxi/main/maplestory_sprites/`

### 装备加载逻辑（无需修改）
- `getIconUrl(folder, id)` — 生成 GitHub CDN URL
- `equipItem(slot, id)` — 切换装备图片显示/隐藏
- 装备6个槽位：hat/top/bottom/cape/face/eyes，z-index 已定义好层叠顺序

### 角色位置和大小
```css
.char-wrapper { right: 20px; bottom: 110px; width: 120px; height: 160px; }
.char-base    { width: 80px; height: 120px; } /* 主体高度120px */
/* 装备叠放位置 */
#equip-hat    { bottom: 100px; z-index: 6; }
#equip-eyes  { bottom: 100px; z-index: 6; }
#equip-face  { bottom: 90px;  z-index: 5; }
#equip-top   { bottom: 30px;  z-index: 3; }
#equip-bottom{ bottom: 0;      z-index: 2; }
#equip-cape  { bottom: 0;      z-index: 0; }
```

---

## 四、AI角色底稿获取

**存放位置**：
```
D:\ComfyUI\output\
├── dresscode_girl_base_00001_.png   # 请上传到 GitHub
└── dresscode_boy_base_00001_.png    # 请上传到 GitHub
```

**上传后需要的 GitHub 路径**：
```
kexvefuxi/char_imgs/girl_base.png
kexvefuxi/char_imgs/boy_base.png
```

---

## 五、优先级建议

| 优先级 | 任务 | 工作量 | 说明 |
|--------|------|--------|------|
| P0 | 替换角色底图为AI绘制图 | 小 | 核心视觉改进 |
| P0 | 添加站立呼吸动画 | 极小 | CSS 两行代码 |
| P1 | 答题动画（正确/错误反馈） | 小 | 需找到各年级答题逻辑位置 |
| P2 | 换装面板美化 | 中 | 可后续迭代 |

---

## 六、遇到问题找我

如果遇到：
- CSS 定位问题 → 找我
- 动画逻辑问题 → 找我
- GitHub 上传问题 → 找我
- 需要生成更多角色图 → 我可以随时调用 ComfyUI

直接甩过来，我来搞！🐉

# 2026-06-06

## v713 云优先存档架构改造
- **文件**: `1/1/index_v712.html`（347KB, 8045行 → 改造后 +80/-19 行）
- **Commit**: `65ea360`，已推送 GitHub

### 改造内容
1. **saveUserData() 云优先 + 重试**：先 await saveToCloudWithRetry（3次，1.5s→3s退避），成功后写 localStorage 镜像；全部失败弹红色 Toast「存档上传失败，请检查网络后手动同步」。保留 skipCloud 参数、ladderPoints/ladderTier 等所有 v712 字段。
2. **loadUserData() 三重 Toast 反馈**：登录时「正在同步云端存档...」→ loadFromCloud(throwOnError=true) → 有数据「云端存档同步完成」/ 空「使用本地数据」/ 异常「网络异常，使用本地存档」
3. **loadFromCloud() throwOnError 参数**：true 时网络异常抛出，区分「云端空」和「网络故障」
4. **60s 心跳自检**：startHeartbeat() / stopHeartbeat()，在 loadUserData.finally() 启动，logout/loginAsGuest 清除
5. **版本号更新**：PAGE_VERSION → '20260606', title → v713

### 验证
- JS 语法检查：`SYNTAX OK`
- Git push：成功 → `main -> main`

## v713 bug修复包（第二轮）
- **Commit**: `573c04a`，已推送 GitHub
- **文件**: `1/1/index_v712.html` + `pet-system.html` + `tower.html` + `xuanba.html`

### 修复内容
1. **🔴 宠物刷积分漏洞**：`pet_points_change` 收到后立即写 localStorage（不等云端），防止云端失败后重登积分恢复
2. **🟡 宠物碎片不显示**：`init_pet` 消息处理后追加 `renderFragments()`，确保碎片数据加载后刷新UI
3. **🟡 爬塔死亡复活**：`goHome()` 中增加 HP <= 0 判断 → 清除进度而非保存，阻止死亡后返回再继续
4. **爬塔换牌功能**：每局送 3 次免费换牌（`run.rerollLeft=3`），Reward 界面加「换一批」按钮
5. **爬塔HP日志**：`calcPlayerStats` 增加 HP 加成明细日志（装备HP/宠物防御/buffHP）
6. **🧠 校队选拔排行榜**：新增 tab + renderXuanbaRankList + pushRankingData 加 xuanba 字段 + xuanba_result 捕获 RA + xuanba.html 传递 percentile


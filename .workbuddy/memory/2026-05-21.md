# 2026-05-21 工作日志

## _parseXlsx() 命题卷分析适配新版题库模板 (08:10)
- **问题**: fenxi.html "上传命题卷"按钮只认旧格式表头（题号/题型），上传新版题库时报"未找到表头"
- **根因**: `_parseXlsx()` (L42521) 是独立解析器（不同于 loadQuestionBank），仅检测"题号+题型"
- **修复**:
  - 新增版本检测：表头'课程序号'→新版，'题号'→旧版
  - 新版列映射：题号从备注提取"第N题"、选项从分散列（选项A-D）合并
  - 向下兼容旧版
- **部署**: Commit `9fb4652` → main ✅

## 服务器同步数据清空 (08:30)
- 清空 `/data/kexvefuxi/student_info.json` → `{}`
- 清空 `/data/kexvefuxi/questionbank_6.json` → `[]`
- 备份在 `/data/kexvefuxi/recovery_backups/`

## fenxi.html 极速瘦身 1.9MB→179KB (08:45) ✅
- **问题**: fenxi.html 1.9MB/46728行，加载超慢
- **根因**: 3个巨型库内联阻塞渲染
- **改造**:
  - xlsx.js: 内联→CDN `cdn.sheetjs.com/xlsx-0.20.3`
  - JSZip: 内联→CDN `cdnjs.cloudflare.com/jszip-3.10.1`  
  - docx.js+xml2js: 删除（400KB死代码，主应用从未调用docx.*函数）
  - 修复 `typeof docx` 依赖检查
- **效果**: 179KB/6417行，缩减90%，加载速度提升10倍+
- **部署**: Commit `6cbddae` → main ✅

## questionbank 500错误修复 (09:30)
- **根因**: 清空时 questionbank_6.json 写成 `[]`（数组），API期望 `{"questions": []}`（对象）
- **修复**: 改为正确格式 + 重启API

## syncFromCloudData 去重修复 (10:00) ✅
- **问题**: 旧同步产生"📌 题目#6（无题库）"占位，新同步有真实题目但去重key不同（question文本 vs questionId），旧记录残留
- **修复**:
  - 去重key改为 `questionId|correctAnswer`（稳定），不再用question文本
  - 新记录含真实题目时，自动清除同questionId的旧占位记录
  - 新记录直接覆盖旧记录（服务器权威来源）
- **部署**: Commit `ac29655` → main ✅

## 错题集账号隔离修复 (12:20) ✅
- **问题**: 学生A登录→退出→学生B登录，错题本仍显示A的错题
- **根因排查**:
  | 检查点 | 状态 | 详情 |
  |--------|------|------|
  | 1.切换账号gameState重置 | ❌ | loginWithId/logout/loginAsGuest 均未重置 wrongQuestions/syncedWrongQuestions/cloudQBank 等 |
  | 2.localStorage studentId | ✅ | saveUserData/loadUserData 使用 scienceGame_user_+studentId |
  | 3.显示错题过滤 | ❌ | renderWrongQuestionList 直接读 gameState，无 studentId 校验 |
  | 4.服务器studentId过滤 | ✅ | syncFromCloudData 用 gameState.studentId 请求 API |
- **致命调用链**: logout不清错题 → loginWithId不清错题 → B无本地存档 → applyUserData被跳过 → gameState.wrongQuestions残留A的数据
- **修复** (`26ab3b9`):
  - `loginWithId()`: 新增重置 wrongQuestions/syncedWrongQuestions/cloudQBank/lastSyncTime/totalQuestionsAnswered/totalCorrectAnswers/retrySuccessCount/towerHighestFloor/towerCoins/ladderBestScore，重置 _wrongQuestionsLoaded 渲染缓存
  - `logout()`: 同 loginWithId 的完整重置
  - `loginAsGuest()`: 同 loginWithId 的完整重置（游客之前也漏了）
- **影响范围**: `1/1/index_v705.html` +44行

## 天梯排行榜控制板报错修复 (14:34) ✅
- **问题**: 学生端ladder.html排行榜页面显示异常，API端点不存在
- **根因**: ladder.html 请求 `/api/ladder/ranking/score/` 和 `/api/ladder/ranking/accuracy/`，但服务器 api.py 从未实现这些端点
- **修复 3 项**:
  1. 🔴 **服务器 api.py** — 新增3个 ladder 端点（v705_ladder）:
     - `GET /api/ladder/ranking/score/<grade>` 积分榜（按score降序）
     - `GET /api/ladder/ranking/accuracy/<grade>` 正确率榜（按正确率降序）
     - `POST /api/ladder/ranking` 提交排行数据
     - 数据存储到 `rankings_ladder.json`（按年级分组）
     - 服务端高积分保护（新分≤旧分时保留旧分）
  2. 🔴 **服务器 api.py** — 修复 `_write_json` 原子写入竞态:
     - 旧: `.tmp` 统一命名 → 并发请求共享同一tmp文件 → 第一个rename移走后第二个报FileNotFoundError
     - 新: `.tmp.{os.getpid()}` 按进程PID命名 → 隔离并发请求
     - 改用 `os.replace()` 替代 `shutil.move()`（POSIX原子rename）
  3. 🟡 **ladder.html:1768** — `LADER_API_BASE` → `LADDER_API_BASE` 拼写修复
     - Commit `9f200da`（GitHub连接断开，待push）
- **验证**: 外部HTTPS端点全部正常，积分榜/正确率榜排序正确

## 天梯云数据同步 + 段位显示修复 (15:39) ✅
- **问题1**: 排行榜只有本地数据，无云数据
- **根因1**: `syncSelfToRanking()` 定义了但从未调用 → `/api/ladder/ranking` 无数据
- **修复1**: ladder.html 挑战结束后调用 `syncSelfToRanking().catch()`
- **问题2**: 天梯云存档失败
- **根因2**: `/api/ladder/profile` 和 `/api/ladder/profile/{studentId}` 端点不存在
- **修复2**: api.py 新增 profile 端点（GET/POST），存储到 `ladder_profiles.json`，智能合并取最大值
- **问题3**: 主系统状态栏不显示天梯段位
- **根因3**: `ladder_change` postMessage 发送了 tier，但父页面丢弃了；gameState 无 ladderPoints/ladderTier
- **修复3**: index_v705.html 4处改动:
  - 状态栏 HTML 新增天梯段位项（icon + 名称 + 子段）
  - `ladder_change` 处理器存储 tier + 调用 updateStatusBar()
  - `updateStatusBar()` 显示天梯段位
  - gameState 初始化 + mergeUserData + loadUserData + 登出重置 补全 ladderPoints/ladderTier
  - 云存档 saveUserData payload 新增 ladderPoints/ladderTier
- **部署**: api.py 已部署验证 ✅（备份 api.py.bak_v705_profile）
- **前端**: Commit `9257ae8`（ladder.html + index_v705.html, +46行）
- **⚠️ git push 阻塞**: 代理 127.0.0.1:7897 卡 git HTTPS 连接，直连被墙，待换网络后 push
- **后续**: 更新网络后 `HTTPS_PROXY= HTTP_PROXY= git push` 成功（代理环境变量是罪魁）
- **备份**: `/data/api.py.bak_v705_ladder`

## 成绩分析同步Bug排查 — 学生数据同步失败 (12:20-14:30)
- **问题**: fenxi.html 同步到复习系统显示"学生0人"，错题集无法显示题目
- **根因分析**:
  1. `syncToReviewSystem()` (L3015): `this.data.students` 为空 → `students.length===0` → 跳过fetch → 服务器无数据
  2. `syncStudents()` (L6296): 遍历 `this.data.classes[].students`，但 `parseWorkbook` 创建的 classes 不含 `.students` 子数组 → 永远跳过
  3. `answers` 格式错误: 存为数组 `['A','B',null,...]`，`s.answers||{}` 不触发（`[]` 是 truthy）→ API 收不到对象格式
  4. 学生端 `syncFromCloudData()` 缺诊断提示 → 无错题时只显示"0条"，不提示原因
- **修复 (fenxi.html, commit a04186c)**:
  - `syncToReviewSystem()`: 添加诊断日志(toast+console)，显示 `this.data.students`/`classes`/`questionBank` 状态
  - `syncToReviewSystem()`: 修复 answers 数组→对象 `{题号:答案}` 转换
  - `syncToReviewSystem()`: 添加网络fetch错误catch + 当students为空时toast提示
  - `syncStudents()`: 重构为遍历 `this.data.students`（修复 classes 无 students 子数组bug）
  - `syncStudents()`: 统一 answers 格式转换，修复 wrongQuestions 类型兼容
- **修复 (index_v705.html, commit d2a2423)**:
  - `syncFromCloudData()`: console.log 诊断学生信息拉取状态
  - 区分3种同步结果toast并给出明确操作指引
  - 代码已提交但GitHub推送因连接断开暂未完成
- **服务端验证**:
  - `/api/students/batch` 端点存在且正常（`{"count":1,"ok":true}`）
  - `/api/student/info/{sid}` 正常读写
  - SSL(443端口)握手失败（可能仅本机网络问题），直接端口5000访问正常
- **下一步**: 需要用户在 fenxi.html 强刷新后重新同步，查看诊断输出

## 爬塔装备继承+刷新按钮 (16:55) ✅
- **问题1**: 爬塔不继承装备攻击力
- **根因1**: tower 读 `scienceGame6_user_{id}` / `bpEquipped_6_{id}`，主系统存 `scienceGame_user_{id}` / `bpEquipped_{id}`，存储键不匹配
- **修复1**: `getTowerEquipment()` + `getTowerEquipped()` 加主存储 fallback
- **问题2**: 回血卡 — 代码已有 HP 变化反馈（之前已修），确认正常
- **问题3**: Chrome 卡死
- **修复3**: Battle 页右上角 🔄 刷新按钮
- **问题4**: Win7 360浏览器 ES6不兼容 → 未修
- **部署**: Commit `4634c19` → main ✅

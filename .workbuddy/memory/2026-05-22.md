# 2026-05-22 工作日志

## 云存档Bug综合排查与P0修复

### 排查发现
1. **students.json 当前有效** (480KB/18条)，但曾反复损坏（corrupted备份1.1-1.2MB）
2. **服务端 API 完全缺失智能合并** — `data[key] = body` 直接覆盖，无 Math.max/去重/零分守卫
3. **_write_json_internal 竞态未修复** — 统一 `.tmp` + `shutil.move()`，v705_ladder 的 `os.replace()+PID` 修复仅应用于 ladder 端点
4. **saveUserData() 触发过于频繁** — 11处调用，applyUserData 末尾无条件触发 saveToCloud
5. **日志证据**: FileNotFoundError: students.json.tmp 反复出现，JSON 损坏报告连续5次

### P0 修复（已部署 v706）
- 数据恢复: 从6个文件恢复 18→52 条记录
- 竞态修复: `os.replace()` + PID 隔离
- 智能合并: 完整的 v706 合并逻辑（Math.max/装备去重/成就并集/错题合并/零分守卫）
- 部署验证: curl 健康检查 OK，零分守卫测试通过（100分不会被0分覆盖）
- 备份: `/data/api.py.bak_v705_profile`

### 数据损失
- 548名学生有排名/个人信息，但仅52人有完整游戏存档
- 大部分存档在 5月14-15日 JSON 损坏中永久丢失
- 补救: 学生重新登录后将自动从 localStorage 恢复并上传（现有了合并保护）

### 尚未修复 (P1)
### P1 修复（14:03 已完成）
- saveUserData 新增 skipCloud 参数
- applyUserData 调 saveUserData(true)，仅存 localStorage
- manualCloudDownload/loadUserData 双重上传消除
- commit ad8d642，已推送 GitHub

### 爬塔bug修复（~15:55）
- maxHPBoost 实际应用到 calcPlayerStats（之前定义了但从未使用）
- showNextQuestion 空值保护（题库为空时不卡死，显示返回首页按钮）
- 战斗页新增「🏠 退出」按钮 + goHome 战斗中保存进度
- 经 git diff 验证：4634c19（爬塔修复）的改动未被回退，我改的 index_v705.html 不涉及 tower.html
- commit fdede1c

# 2026-05-13

## Bug修复：v602宠物弹窗空白
- **问题**: `1/1/index_v602.html` 宠物弹窗打开后空白
- **原因**: iframe 标签 `<iframe id="petIframe" >` 缺少 `src="../../pet-system.html"` 属性，iframe 加载 about:blank
- **修复**: 添加 `src="../../pet-system.html"`
- **影响范围**: 仅 `index_v602.html`，其他年级和 `index.html` 均正常

## 新版本：V604 创建 + 状态栏学生名字显示
- 从 `index.html` 复制创建 `1/1/index_v604.html`
- **新增 `data/student-names.js`**: 学生学号→姓名映射表，提供 `getStudentName(id)` 函数
- **V604 改动**:
  1. 加载 `<script src="../../data/student-names.js">`
  2. 登录时 `gameState.studentName = getStudentName(id)` 查名字
  3. 状态栏：有名字显示名字、没名字回退显示学号 (`gameState.studentName \|\| gameState.studentId`)
  4. 选课欢迎语：`(gameState.studentName \|\| id) + ' 同学'`
- 名字映射目前为空，需手动在 `data/student-names.js` 的 `STUDENT_NAMES` 对象中添加学号→姓名

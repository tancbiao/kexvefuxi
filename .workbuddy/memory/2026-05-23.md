# 2026-05-23 工作日志

## 技能卡牌 + 怪物血量（13:11）
- 🦁 战吼(LV1-7): 攻击时概率触发，临时+ATK%+护盾，2回合后过期
- 🏹 多重射击(LV1-7): 攻击时概率触发，连射2-4次，每次独立暴击
- 可升级: 再次选中即升级，满级LV7不再刷出
- 怪物HP: 100层以上hpMult×2，boss以100层为基准指数增长(110=100×1.5)
- 存档: skillLevels已加入saveTowerProgress/continueTower
- commit 2bf11ea, 7548927 已推送

## S0赛季结算（16:15）
- 登录时检测 tower6_{id}.highestFloor > 100
- 弹窗通知 + 奖励：随机神话装备(生成quality 7) + 随机UR宠物(21-24) + 5万积分
- 层数重置到100，清除爬塔进度
- 一次性标记 s0_season_reward_claimed_{id}
- 宠物: 直接写入petSystemStateV2并自动上阵
- commit 5373a73 已推送

## 宠物碎片同步
- 卡牌匹配修复后petPiece应恢复正常（apply之前因emoji匹配失败从未执行）
- 父页面tower_change处理器已正确合并fragments
- 如仍有问题需进一步排查

## 云存档状态
- students.json: 148条（自动恢复中），零错误日志，v706 稳定

## 爬塔修复（全天多次提交）
- 死亡后禁止继续：onGameOver 清除进度 + 按钮仅 HP>0 + continueTower HP<=0 保护
- 初始攻击力：run 初始化 10→20，calcPlayerStats 最低 5→20，continueTower 最低 20
- 材料题显示：\n\n→<br><br> + 【材料】金色高亮 + 配图缺失提示
- .q-text CSS：max-height 40vh 可滚动，line-height 1.7
- 鸟足插图：从桌面 Excel 提取 image2.png，base64 嵌入 x12 题
- 回血卡牌：heal30/recoverAll 加 updatePlayerHPBar() 即时刷新
- 题目答案修正：圆珠笔钢珠 C→B，x22 answer:2→1

## 项目迁移（11:30）
- 主目录从 WPS 云盘迁至 D:\kexvefuxi\
- 旧路径废弃：C:\Users\tanc\Documents\WPSDrive\362543761\WPS云盘\谭政\科学复习系统\_kexvefuxi\
- D盘已同步至 a134fb8，需 git pull 拉取最新 40f9d69
- GitHub: tancbiao/kexvefuxi main branch

## 回血卡牌诊断（11:46）
- hpMax30 补上遗漏的 updatePlayerHPBar() 调用
- selectCard 和 enterFloor 加 console.log 诊断日志
- commit 51d614c 已推送

## 返回按钮 + ATK诊断 + WPS检查（12:01）
- 返回按钮消失根因: enterFloor line 2139 写死了 display:none → 改为 inline-block
- ATK无效诊断: calcPlayerStats 加 console.log（装备数/槽位/equipATK/petATK/最终ATK）
- WPS确认: D:\kexvefuxi 不在 WPS 云盘路径下，不会被WPS还原。旧同步路径已废弃
- commit 59d2afb 已推送（需代理）

## hpMax30修复（12:56）
- **根因**: 之前 `hpMax30` 只加 `run.playerHP + 30`，不是回满。用户当前HP可能已受损（如100/160），加了30变130，但期望回满到190
- **修复**: 改为 `run.playerMaxHP += 30; run.playerHP = run.playerMaxHP` — 加30上限并回满到新上限
- 也加了 console.log 追踪
- commit 9587bc0 已推送

## 装备ATK: 确认正常
- `calcPlayerStats`: 每1点str/dex/int = +1ATK，equipATK + petATK，最低20
- 卡牌匹配修复后 atkBoost15 等buff也正常生效
- 攻击力显示 = calcCurrentATK(run.playerATK × buffs)
- 根 index.html: 用 JavaScript Date() 动态生成版本号，不再手动改日期。每次访问自动跳转当天最新版
- index_v705.html: 顶部加 PAGE_VERSION 常量 + URL 自检脚本。学生书签?v=旧版本→自动重定向到新版本
- **过渡期**: 已缓存旧版的学生首次需 Ctrl+F5 刷新一次，之后每次更新自动跳转
- **以后每次部署**: 只需改 index_v705.html 顶部的 PAGE_VERSION 常量
- commit acf003b 已推送

## 题库答案修正（12:24）
- shared-exam-bank.js: 红树林-食物网题 id=29 answer 2→1（B选项正确）
- shared-exam-bank.js: 红树林-鹭鸟与鱼题 id=26 answer 1→2（C/捕食关系）
- commit ad6b5f6 已推送

## 🔴 根因：卡牌匹配失败（11:57）
- selectCard 通过 textContent 匹配卡牌名称，但 innerHTML 渲染时 emoji 编码不一致
- 例如 DOM 中 textContent = "生命上限"，CARD_POOL 中 name = "❤️ 生命上限" → 不匹配
- 修复：改用 data-card-id 属性直接匹配 id（hpMax30/atkBoost15 等），不再依赖 textContent
- **这解释了所有卡牌效果"无效"的问题**——heal30/recoverAll/hpMax30/atkBoost15/所有卡牌的 apply() 从未被执行
- commit 8826406 已推送

## 交接文档更新
- HANDOVER.md 全面重写，覆盖最新架构、路径、v706修复、Git状态、CDN策略

## 待处理
- D盘项目需 git pull 拉取回血卡牌修复(40f9d69)
- 材料显示如有问题需进一步排查（题库前10题无材料是正常的）
- WPS云盘旧副本可删除

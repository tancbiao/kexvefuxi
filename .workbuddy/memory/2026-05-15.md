# 2026-05-15

## AI讲解 bug 修复
- **Bug**: AI讲解弹窗中错题数始终显示 0
- **根因**: `ai-tutor.js` 使用了 `window.gameState`，但 `gameState` 在 `index.html` 中是用 `const` 声明的（line 1609），不会挂到 `window` 上，`window.gameState` 永远是 `undefined`
- **修复 1**: `js/ai-tutor.js` — 4 处 `window.gameState` 改为直接引用 `gameState`（line 227, 261, 284, 285）
- **修复 2**: `1/1/index.html` — 添加 `<script src="../../js/ai-tutor.js">`（之前只加在 `index_v702.html`，主文件 `index.html` 没加载该脚本）
- **Commit**: `9e80398`，已推送到 GitHub

## 崩溃根因分析
- 上一会话崩溃（疯狂点击发送/停止）是因为在编辑大文件 `index_v702.html`（5300+ 行）时陷入 Edit 失败 → 重试 → 死循环
- 后续编辑大文件必须遵守 `kexvefuxi-project` skill 中的"致命陷阱"规范：分段 Read + grep 确认 + worktree 隔离
